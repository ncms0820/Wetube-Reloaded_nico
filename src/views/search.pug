
extends base.pug
include mixins/video

block content 
    form(method="GET")
        input(placeholer="Search by title", name="keyword" type="text")
        input(type="submit", value="Search now")
    div 
        each video in videos
            +video(video)